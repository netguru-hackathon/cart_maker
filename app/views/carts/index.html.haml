%h5 Available carts:

%table.highlight
  %thead
    %tr
      %th City name
      %th Actions

  %tbody
    - @carts.each do |cart|
      %tr
        %td= link_to cart.name, cart
        %td
          %a.tooltipped{href: add_products_cart_path(cart), data: { tooltip: 'Add product to cart'} }
            %i.material-icons
              add
          %a.tooltipped{href: cart_path(cart), data: { method: 'delete', tooltip: 'Delete cart' } }
            %i.material-icons
              remove

%br

%a.btn.waves-effect.waves-light{href: new_cart_path}
  Create new cart
  %i.material-icons.right
    send
