%ul.collection.highlight
  - @cart.products.each do |product|
    %li.collection-item.avatar
      %img{'src': product.image, 'alt': product.name, 'class': 'circle'}
      %span.title
        = product.name
      %p
        Quantity:
        %span.product-quantity
          = product.quantity
        %br
        Price:
        %span.product-price
          = product.bulk_price
      %a{'href': '#!', 'class': 'secondary-content'}
        %i.material-icons.inc-product
          add
        %i.material-icons.dec-product
          remove

.row
  .col.s12.m4.l2
    %a.btn.waves-effect.waves-light.new-product-btn{href: add_products_cart_path(cart)}
      Add new product
      %i.material-icons.right
        send
  .col.s12.m4.l8.divider
  .col.s12.m4.l2
    %button.btn.waves-effect.waves-light.sync-btn
      Sync
      %i.material-icons.right
        send
